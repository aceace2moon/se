# [WorkDrive] 프로젝트 테스트 결과 보고서

**문서번호**: [팀명]테스트결과서_260618_Doc-001  
**작성자**: [사용자 성명]  
**테스트 기간**: 2026-06-11 ~ 2026-06-17  

---

## 1. 개요 (Introduction)

### 1.1 테스트 목적
본 보고서는 WorkDrive(클라우드 파일 공유 시스템)의 핵심 요구사항(기능적/비기능적)이 올바르게 구현되었는지 확인하고, 시스템의 안정성, 가용성, 성능 및 보안성이 사전에 설정된 품질 목표에 부합하는지 검증하는 것을 목적으로 한다.

### 1.2 테스트 대상 범위
- 회원 관리 및 로그인 기능
- 파일 업로드 및 대용량 파일 청크(Chunk) 분할 전송 기능
- 파일 공유 링크 생성 및 접근 권한(읽기/쓰기) 제어 기능
- 비인가 접근 차단 및 실시간 관측(Observability) 시스템

---

## 2. 테스트 환경 (Test Environment)

샘플 가이드라인에 따라 테스트가 진행된 하드웨어 및 소프트웨어 사양을 정의한다.

### 2.1 하드웨어 환경
- **테스트 서버**: CPU Intel Xeon 8 Core, RAM 16GB, NVMe SSD 512GB (총 10대 클러스터 구조)
- **클라이언트 기기**: Intel i5 8세대 CPU, RAM 8GB, OS Windows 10

### 2.2 소프트웨어 환경
- **OS**: Ubuntu 22.04 LTS (서버 환경)
- **개발 프레임워크**: Python / Java Spring Boot 3.x
- **인프라 및 도구**: Docker Containers, Jenkins (CI/CD), Prometheus & Grafana (모니터링)

---

## 3. 테스트 디자인 및 테스트 케이스 (Test Cases)

### 3.1 기능 테스트 (Functional Testing)

| 테스트 ID | 테스트 항목 | 테스트 시나리오 (입력 및 절차) | 예상 결과 | 수행 결과 |
| :--- | :--- | :--- | :--- | :--- |
| **TC-F-001** | 회원 가입 | 이메일 형식 및 암호화된 비밀번호를 입력하여 계정을 생성한다. | DB에 비밀번호가 해시화되어 안전하게 저장됨. | **PASS** |
| **TC-F-002** | 파일 업로드 | 2GB 크기의 대용량 파일을 웹 UI에 드래그 앤 드롭한다. | 파일이 지정된 크기의 청크 단위로 자동 분할되어 병렬 업로드됨. | **PASS** |
| **TC-F-003** | 공유 링크 생성 | 특정 파일에 대해 '읽기 전용' 권한 및 유효기간 7일을 설정하여 링크를 생성한다. | 만료일 정보가 포함된 토큰화된 URL이 정상 발급됨. | **PASS** |
| **TC-F-004** | 링크 권한 제어 | '읽기 전용' 링크로 접속한 사용자가 파일 수정을 시도한다. | 시스템이 수정 요청을 거부하고 권한 오류 메시지를 출력함. | **PASS** |

### 3.2 비기능 및 품질 목표 테스트 (Non-Functional Testing)

| 테스트 ID | 품질 요소 | 테스트 시나리오 및 부하 조건 | 품질 목표 (기준) | 측정 결과 | 판정 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **TC-N-001** | **성능** (Performance) | 200명의 동시 접속자가 임의의 파일 검색 API를 10분간 지속 호출한다. | 검색 응답시간 $P_{95}$ 기준 **2초 이내** | $P_{95}$ = **1.45초** | **PASS** |
| **TC-N-002** | **가용성** (Availability) | 임의로 분산 스토리지 노드 2대를 다운시킨 후 파일 접근을 시도한다. | 다운타임 없이 가동률 **99.5% 이상** 보장 | 장애 조치(Failover) 작동, 가동률 **99.9%** | **PASS** |
| **TC-N-003** | **보안** (Security) | 위조된 인증 토큰을 사용하여 파일 다운로드 API 1,000건을 무작위 호출한다. | 비인가 접근 차단율 **100%** | 1,000건 전량 차단 성공 (**100%**) | **PASS** |
| **TC-N-004** | **관측성** (Observability) | 비인가 접근 차단 이벤트 발생 시 모니터링 시스템의 로그 전송 속도를 측정한다. | 로그 수집 지연 **5초 이하** | 평균 지연 시간 **3.2초** | **PASS** |

---

## 4. 테스트 결과 요약 (Test Result Summary)

### 4.1 종합 수행 통계
- **총 테스트 케이스 수**: 8건
- **성공 (PASS)**: 8건
- **실패 (FAIL)**: 0건
- **요구사항 달성률**: 100%

### 4.2 결함(Bug) 요약 및 조치 이력
- **발견된 결함 수**: 총 1건
- **결함 내용**: 대용량 파일 업로드 중 네트워크 순간 단절 시 이어받기(재시도) 로직이 무한 루프에 빠지는 현상 발생 (TC-F-002 연관).
- **조치 내용**: 최대 재시도 횟수를 5회로 제한하고, 5회 실패 시 사용자에게 알림창을 띄우도록 예외 처리 코드를 보완하여 최종 재테스트 통과(PASS).

---

## 5. 결론 (Conclusion)
WorkDrive 시스템의 핵심 기능과 비기능 품질 목표에 대한 종합 테스트를 수행한 결과, 설계 단계에서 정의했던 성능($P_{95}$ 2초 이내), 보안성(비인가 차단 100%), 관측 가능성(지연 5초 이하) 등의 모든 베이스라인 기준을 안정적으로 충족함을 확인하였다. 본 프로젝트는 배포 단계로 진행하기에 적합한 품질 수준을 갖춘 것으로 판정한다.